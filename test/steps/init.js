"use strict";

const init = async () => {
    require("dotenv").config()
}
module.exports = init;


/* import dotenv from "dotenv";

const init = async () => {
  dotenv.config();
};

export default init; */